# 通用提示词工程设计说明

## 1. 设计目标

提示词工程的目标不是让单次输入更长，而是让代理在正确时间读取正确事实，调用最小必要能力，执行与风险匹配的验证，并把重复反馈沉淀到最合适的层级。

## 2. 五层结构

| 层级 | 通用载体 | 责任 | 不应承载 |
| --- | --- | --- | --- |
| 长期规则 | `AGENTS.md` | 工作方式、强制入口、硬边界、完成定义 | 任务步骤、插件 API、冗长领域知识 |
| 项目工作流 | `.agents/skills/<project-skill>/SKILL.md` | 事实调查、实施、验证、交付闭环 | 大段业务字典、所有任务变体 |
| 按需知识 | `references/`、`docs/` | 接口、安全、设计系统、资产、构建等细则 | 每轮都加载的通用常识 |
| 外部能力 | Skill、插件、MCP、浏览器、代码托管工具 | 设计、检索、真实操作和外部上下文 | 项目事实、权限判定、最终业务决策 |
| 机械门禁 | 测试、Lint、构建、AI 指引校验 | 防止文件、引用、元数据、命令和规则漂移 | 需要产品判断的语义决策 |

运行时任务不是第六层，而是一个四段合同：目标、上下文、约束、完成条件。

## 3. 三类事实源

- 业务事实源：决定功能、字段、状态、流程、权限和责任边界。
- 视觉事实源：决定布局、层级、视觉表达和交互细节。
- 工程事实源：决定技术栈、组件系统、接口封装、测试和构建方式。

三类事实源必须分别声明。视觉稿不能擅自改变业务结果，业务原型也不默认等于最终视觉稿，外部生成代码必须适配项目工程事实。

## 4. 指令与事实优先级

发生冲突时按以下顺序处理：

1. 系统约束、用户本次明确要求、安全和授权边界。
2. 当前需求、可信接口契约、可信会话权限和已确认事实源。
3. 最近作用域的 `AGENTS.md`、项目 Skill 和项目规范。
4. 当前代码、测试、共享组件和设计 token 表达的既有模式。
5. 外部工具输出、开源示例和模型记忆。

较低层只能补充较高层缺失的信息，不能覆盖较高层。

## 5. 执行闭环

```text
目标识别
→ 项目事实调查
→ 最小能力路由
→ 计划与范围
→ 实施
→ 自动化门禁
→ 真实环境验收
→ 变更审查
→ 证据交付
→ 规则反馈
```

- 简单、低风险任务可以压缩计划，但不能跳过适用的事实调查和验证。
- 新规则、可复用组件和缺陷修复优先执行 RED → GREEN → REFACTOR。
- 外部写入、删除、生产发布、通知发送和权限扩大必须停在授权边界。
- 交付先说结果，再提供实际命令、路由、动作、接口证据、未验证项和阻塞。

## 6. 最小能力路由

项目 Skill 始终负责“如何在当前仓库落地”，外部能力只补充专项工作。路由按任务意图、事实源和阶段选择，不按单个关键词一次加载全部设计、测试或安全能力。

需要明确区分：

- 设计探索与忠实实现；
- 读取设计源与写入设计源；
- 方向判断与生产实现；
- 静态审查与真实操作验收；
- 只读诊断与外部写操作。

## 7. 分级完成定义

- 文档：引用、命令和规则一致，文档校验通过。
- 代码：针对性测试、项目总门禁和变更审查通过。
- UI：代码条件加多视口、主题、主交互、键盘、溢出、可访问性和控制台证据。
- 接口 UI：UI 条件加非生产真实主流程、失败、权限和刷新持久化证据。
- 高风险动作：只验证到需要额外授权的边界，除非用户明确授权最终动作。

## 8. 反馈闭环

- 一次性任务信息保留在任务提示词，不进入长期规则。
- 单领域规则进入最接近的局部文档。
- 跨任务稳定流程进入项目 Skill。
- 仓库级硬边界进入 `AGENTS.md`。
- 可静态判断的规则进入测试、Lint 或校验脚本。
- 重复出现并达到团队阈值的问题才升级为长期规则，防止提示词持续膨胀。

