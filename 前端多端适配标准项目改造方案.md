# 前端多端适配标准项目改造方案

## 1. 方案目标

本方案用于改造当前“通用提示词工程”项目，使其更贴近 Codex 的生态和使用方式，支持：

- Codex 能力安装、连接和验证。
- 新项目初始化和项目规则接入。
- 基于原型图、导出 HTML、原始网页 HTML、截图、Figma、接口文档和原始需求的前端开发。
- 新项目开发与既有项目增量开发。
- 桌面端、平板端和移动端的多端适配与真实验收。
- 事实、设计、工程、权限和验收证据的分层管理。

方案原则：

1. 用 Codex Skill 承载长期、可复用的工作流。
2. 用 `AGENTS.md` 承载项目级硬约束。
3. 用 `docs/PROJECT_PROFILE.md` 承载项目事实。
4. 用任务文件承载一次性需求，不把所有内容堆进一个超长 Prompt。
5. 按任务选择最小能力组合，不默认加载完整能力集。
6. 安装、启用、连接、可发现和任务可用必须分开记录。
7. 视觉来源不能覆盖业务事实，外部工具输出不能替代项目事实。
8. 所有关键结论都要有证据，无法确认的内容必须保留为未验证状态。

## 2. 当前方案的职责理解

### 2.1 `给Codex的安装提示词.md`

该文件当前承担 Codex 能力安装的三阶段流程：

1. **安装前审计**：只读检查当前 Codex 环境、插件、宿主内置能力、独立 Skill、项目 Skill 和连接状态，按照任务上下文推荐能力模式，不执行安装。
2. **批准项安装**：只安装用户明确批准的插件和独立 Skill，跳过已安装且来源匹配的项目，不安装宿主内置 Browser 等能力，不自动连接外部服务。
3. **新会话验证**：安装独立 Skill 后开启新 Codex 会话，检查 Skill 是否能被发现、名称和来源是否正确，并验证当前任务的最小能力组合。

它还定义了：

- 插件、宿主内置能力、独立 Skill 和项目 Skill 的分类。
- 核心、UI、React UI、Figma、GitHub 和完整模式。
- Figma、GitHub 的安装状态与连接状态分离。
- 不修改业务代码、项目配置和项目文档的权限边界。
- 安装结果、阻塞原因和用户下一步操作的输出格式。

### 2.2 `给项目初始化的提示词.md`

该文件当前承担项目级初始化流程：

1. 读取项目代码、配置、文档、测试、CI、部署和已有规则，建立事实来源。
2. 在 `docs/PROJECT_PROFILE.md` 不存在时，选择 Vue、React、通用模板或根据仓库事实自动生成画像草案。
3. 在画像已存在时，以现有画像为事实源，不覆盖、不重建，只进行逐字段比较和合并。
4. 为每个字段标记“仓库事实确认”“仓库可推断、需用户确认”“事实缺失、需用户提供”或“不适用”。
5. 等待用户确认高影响字段。
6. 只把已由仓库事实或用户明确确认的内容写入 `docs/PROJECT_PROFILE.md`。
7. 在规则模板接入获得批准后，合并 `AGENTS.md`、项目 Skill、文档、校验配置和脚本。
8. 保护已有业务规则、业务代码、配置、测试和非占位内容，不整目录覆盖。
9. 执行本地校验并输出实际变更、保留文件、未完成项和证据。

## 3. 现有文件与新设计文件映射

### 3.1 `通用模板/docs` 文件映射

| 现有文件 | 新设计文件 | 处理方式 | 职责 |
|---|---|---|---|
| `通用模板/docs/PROJECT_PROFILE.md` | `docs/PROJECT_PROFILE.md` | 保留 | 当前项目事实、技术画像、命令、权限和验收基线 |
| `通用模板/docs/PROJECT_PROFILE_REACT.md` | `templates/project-profile.react.md` | 建议迁移并改名 | React 项目画像模板，不代表当前项目事实 |
| `通用模板/docs/PROJECT_PROFILE_VUE.md` | `templates/project-profile.vue.md` | 建议迁移并改名 | Vue 项目画像模板，不代表当前项目事实 |
| `通用模板/docs/FRONTEND_CONVENTIONS.md` | `docs/FRONTEND_CONVENTIONS.md` | 保留 | 前端工程、目录、组件、样式、响应式和代码约定 |
| `通用模板/docs/AI_CAPABILITY_REQUIREMENTS.md` | `docs/CODEX_CAPABILITIES.md` | 改名 | Codex 插件、宿主能力、独立 Skill、项目 Skill 和连接状态清单 |
| `通用模板/docs/AI_PROMPT_ENGINEERING.md` | `docs/AI_WORKFLOW_PRINCIPLES.md` | 改名 | 事实分层、优先级、任务闭环、反馈沉淀和提示词设计原则 |
| `通用模板/docs/AI_PROJECT_STANDARDS.md` | `docs/AI_PROJECT_STANDARDS.md` | 保留 | AI 参与项目开发时的业务、工程、安全、API 和质量标准 |
| `通用模板/docs/AI_TASK_PROMPT.md` | `docs/AI_TASK_CONTRACT.md` | 改名 | 通用任务合同：目标、上下文、约束、完成条件和证据 |
| `通用模板/docs/AI_PAGE_PROMPT.md` | `docs/AI_FRONTEND_TASK.md` | 改名并扩展 | 前端任务入口，支持多种输入源和任务组合 |
| `通用模板/docs/AI_COMPONENT_CATALOG.md` | `docs/AI_COMPONENT_CATALOG.md` | 保留 | 组件复用、扩展、页面结构和已有资产目录 |
| `通用模板/docs/AI_ACCEPTANCE_EVIDENCE.md` | `docs/AI_ACCEPTANCE_EVIDENCE.md` | 保留并扩展 | 交付证据、视觉验收、接口验收、可访问性和多端验收矩阵 |

### 3.2 两个现有 Prompt 的映射

| 现有文件或内容 | 新设计对应物 | 处理方式 |
|---|---|---|
| `给Codex的安装提示词.md` | `.agents/skills/codex-capability-setup/SKILL.md` | 将长期流程迁移为 Skill 入口 |
| 安装前审计 | `codex-capability-setup/references/setup-phases.md` | 拆出阶段 A |
| 批准项安装 | `codex-capability-setup/references/install-policy.md` | 拆出阶段 B |
| 新会话验证 | `codex-capability-setup/references/verification-checklist.md` | 拆出阶段 C |
| 能力状态表 | `docs/capability-state.yaml` + `references/status-model.md` | 结构化状态记录 |
| 批准安装输入 | `codex-capability-setup/templates/approval-input.md` | 固定用户输入格式 |
| `给项目初始化的提示词.md` | `.agents/skills/bootstrap-project/SKILL.md` | 将项目初始化流程迁移为 Skill |
| 画像字段定义 | `bootstrap-project/references/profile-schema.md` | 提取字段结构和证据要求 |
| 模板选择逻辑 | `bootstrap-project/references/profile-selection.md` | 独立处理 Vue、React、通用和自动生成 |
| 字段确认流程 | `bootstrap-project/references/confirmation-flow.md` | 集中处理确认策略 |
| 文件合并规则 | `bootstrap-project/references/merge-policy.md` | 保护已有文件并处理差异 |
| 初始化后校验 | `bootstrap-project/references/acceptance-checklist.md` | 统一初始化验收 |

### 3.3 建议保留的文件名

以下名称已经具有稳定项目语义，建议保留：

```text
PROJECT_PROFILE.md
FRONTEND_CONVENTIONS.md
AI_PROJECT_STANDARDS.md
AI_COMPONENT_CATALOG.md
AI_ACCEPTANCE_EVIDENCE.md
```

## 4. 新目录结构

```text
项目根目录/
├─ AGENTS.md
├─ .agents/
│  └─ skills/
│     ├─ project-workflow/
│     │  ├─ SKILL.md
│     │  ├─ agents/openai.yaml
│     │  └─ references/task-routing.md
│     ├─ bootstrap-project/
│     │  ├─ SKILL.md
│     │  └─ references/
│     │     ├─ profile-schema.md
│     │     ├─ profile-selection.md
│     │     ├─ confirmation-flow.md
│     │     ├─ merge-policy.md
│     │     ├─ protected-files.md
│     │     └─ acceptance-checklist.md
│     └─ frontend-task/
│        ├─ SKILL.md
│        └─ references/
│           ├─ source-routing.md
│           ├─ prototype-workflow.md
│           ├─ html-workflow.md
│           ├─ screenshot-workflow.md
│           ├─ figma-workflow.md
│           ├─ api-workflow.md
│           ├─ requirement-workflow.md
│           └─ acceptance-matrix.md
├─ docs/
│  ├─ PROJECT_PROFILE.md
│  ├─ FRONTEND_CONVENTIONS.md
│  ├─ AI_PROJECT_STANDARDS.md
│  ├─ AI_COMPONENT_CATALOG.md
│  ├─ AI_ACCEPTANCE_EVIDENCE.md
│  ├─ CODEX_CAPABILITIES.md
│  ├─ AI_WORKFLOW_PRINCIPLES.md
│  ├─ AI_TASK_CONTRACT.md
│  ├─ AI_FRONTEND_TASK.md
│  └─ capability-state.yaml
└─ scripts/                 # 仅当项目明确接入初始化校验时才存在
   ├─ check-ai-guidance.mjs
   └─ lib/
      ├─ ai-guidance-validation.mjs
      └─ ai-guidance-validation.test.mjs
```

如果希望减少迁移范围，可以暂时保留 `docs/PROJECT_PROFILE_REACT.md` 和 `docs/PROJECT_PROFILE_VUE.md`，但必须在文件头明确标记为“画像模板”，不能把它们当作当前项目事实。


### 4.1 模板仓库与目标项目边界

`templates/` 和初始化脚本不能被简单地从目标项目中移除。新项目需要依靠它们完成画像选择、规则生成和校验。正确边界是区分“模板源包”和“初始化后项目副本”，并使用 Codex 专用命名空间避免与业务目录冲突。推荐在改造工具仓库中维护源包：

```text
改造工具仓库/
├─ template-assets/
│  ├─ project-profile.react.md
│  ├─ project-profile.vue.md
│  └─ project-profile.generic.md
└─ initializer/
   ├─ check-ai-guidance.mjs
   └─ lib/ai-guidance-validation.mjs
```

目标项目只接入初始化后真正需要长期维护的文件：

```text
目标项目/
├─ AGENTS.md
├─ .agents/skills/
├─ docs/
└─ scripts/              # 只有项目明确批准接入校验脚本时才创建
```

这样可以避免模板文件名、初始化脚本和项目已有的 `templates/`、`scripts/` 目录发生冲突。若目标项目已经有同名目录，初始化流程必须先输出差异，不得整目录复制或覆盖。

### 4.2 `AGENTS.md` 的生成关系

`AGENTS.md` 不是简单复制的静态模板文件，而是项目初始化阶段基于模板包生成或合并的项目级规则入口：

```text
通用规则模板 + 项目事实 + 用户确认的边界
→ 生成/合并目标项目 AGENTS.md
```

生成时必须：

- 保留目标项目已有 `AGENTS.md` 的规则和局部约束。
- 将通用模板中的工作方式、权限边界和验收要求作为候选内容进行逐段比较。
- 只写入用户批准或项目事实明确支持的内容。
- 将项目专属命令、目录、组件和业务边界写入目标项目规则。
- 不把模板仓库中的路径、示例项目名或示例命令原样写成项目事实。

因此，`bootstrap-project` 的职责是生成和维护目标项目的 `AGENTS.md`，而 `project-workflow` 是被接入后的项目 Skill；两者不是同一个文件，也不是通过全局 Skill 安装互相替代。

## 5. Codex 能力安装设计

### 5.1 能力分类

| 类型 | 示例 | 管理方式 |
|---|---|---|
| Codex 插件 | Product Design、Figma、GitHub | Codex Plugin Management |
| 宿主内置能力 | Browser、Chrome、Computer Use | 只检查当前宿主是否提供 |
| 独立 Skill | TDD、API Design、Security Review、Frontend Design | 固定来源安装到全局 Skill 目录 |
| 项目 Skill | `project-workflow`、`bootstrap-project`、`frontend-task` | 随项目文件接入，由项目规则发现 |

### 5.2 三阶段状态机

```text
缺失
→ 待用户批准
→ 已安装
→ 已启用
→ 已连接（仅适用于外部服务）
→ 新会话可发现
→ 当前任务可用
```

这些状态不可合并。例如：

- 安装 Figma 插件不代表已经连接 Figma。
- Skill 安装成功不代表当前会话已经发现 Skill。
- Browser 可用不代表 Figma 或 GitHub 可用。
- 命令退出码为 0 不代表当前任务能力已经可发现。

### 5.3 阶段 A：安装前审计

只读检查：

- 当前项目的 `AGENTS.md`、`PROJECT_PROFILE.md` 和 `project-workflow`。
- 当前 Codex 可发现的 Skill。
- 插件安装和启用状态。
- Browser、Chrome、Computer Use 等宿主内置能力。
- Figma、GitHub 的安装状态和连接状态。
- 当前任务需要的最小能力组合。

输出应包含：

- 能力类型。
- 精确名称或插件引用。
- 固定来源。
- 安装、启用、连接、可发现和任务可用状态。
- 证据和阻塞原因。
- 推荐安装项、可选项和不建议安装项。
- 可直接复制的批准输入。

阶段 A 不得安装、连接、授权、复制模板、修改项目文件或创建外部对象。

### 5.4 阶段 B：批准后安装

只执行用户批准的精确项目：

- 插件只能通过 Codex Plugin Management 安装。
- 独立 Skill 只能从能力清单中的固定来源安装。
- 已安装且名称、来源和路径匹配的项目跳过，不覆盖。
- Browser 等宿主能力不通过公共插件目录安装。
- Figma 和 GitHub 的 OAuth 或登录必须使用宿主连接流程。
- 不记录令牌、密钥或登录凭据。
- 不因安装成功而声称当前会话已经发现能力。

### 5.5 阶段 C：新会话验证

只有安装了新的独立 Skill，或当前会话无法发现新能力时，才需要新会话。

新会话验证：

- 精确 Skill 名称、`SKILL.md` 元数据和来源。
- 项目级 `project-workflow` 是否可发现。
- 当前任务所需的最小能力是否可用。
- Figma、GitHub 是否仅在需要时连接。
- Browser 是否由宿主提供。

## 6. 项目初始化设计

### 6.1 初始化流程

```text
读取项目事实
→ 选择画像来源
→ 生成画像草案
→ 标记证据和不确定项
→ 用户确认高影响字段
→ 受保护地写入 PROJECT_PROFILE.md
→ 用户批准规则接入
→ 合并 AGENTS.md、项目 Skill、docs 和校验脚本
→ 运行校验
→ 输出证据和未完成项
```

### 6.2 画像字段处理

字段分为四种状态：

- `仓库事实确认`：存在明确文件、配置、代码、命令或测试证据。
- `仓库可推断，需用户确认`：有合理候选值，但会影响架构、权限、交付或体验。
- `事实缺失，需用户提供`：仓库无法支持安全结论。
- `不适用`：由项目事实或用户确认明确排除。

首次初始化优先确认高影响字段：

- 技术栈和运行时版本。
- 开发、构建、测试和部署命令。
- API 来源、认证和权限边界。
- 是否需要 UI 视觉验收。
- 是否使用 Figma 或 GitHub 外部连接。
- 是否接入前端约定。
- 是否接入项目级工作流。

不影响架构和安全的低风险字段可以延后补充。

### 6.3 受保护文件

以下文件不得被模板整文件覆盖：

- `docs/PROJECT_PROFILE.md`：只合并用户确认字段和明确仓库事实。
- 已有 `AGENTS.md`：保留原规则，新增内容必须逐段比较。
- 已有 `.agents/skills/project-workflow/`：保留现有 Skill、元数据和引用。
- 已有 `docs/`、配置、脚本、测试和业务文件：不因模板版本更新而自动替换。

只有目标路径不存在时，才允许从模板创建文件。目标路径已存在时，必须先输出差异并等待确认。

### 6.4 初始化校验

至少检查：

- 不存在未确认占位符。
- Markdown 链接和文档引用有效。
- Skill 元数据和路径一致。
- `ai-guidance.config.mjs` 与校验脚本一致。
- `PROJECT_PROFILE.md` 没有被模板覆盖。
- 项目真实命令可执行，不能把示例命令当作事实。
- 初始化变更未修改业务代码和生产配置。

## 7. 前端任务开发 Skill

新增 `.agents/skills/frontend-task/`，统一处理新项目和增量开发。

### 7.1 任务入口

推荐使用任务文件：

```yaml
task_type: new-page # new-project | new-page | incremental | bug-fix | refactor
sources:
  - screenshot: ./design/users.png
  - api: ./docs/users.openapi.yaml
target:
  route: /users
  path: src/pages/users
constraints:
  preserve_existing_components: true
acceptance:
  - responsive
  - loading
  - empty
  - error
  - keyboard_accessible
```

也支持直接在 Codex 对话中调用：

```text
使用 $frontend-task。
任务类型：新页面 / 增量修改 / 缺陷修复
输入来源：截图、Figma、接口文档
目标：实现用户列表页
路径：src/pages/users
验收：桌面端、移动端、加载态、空态、错误态
```

### 7.2 输入来源路由

| 输入来源 | 主要事实 | 默认能力组合 |
|---|---|---|
| 原型图或导出 HTML | 页面结构、交互和布局 | `project-workflow` + 原型分析 + UI 实现 + Browser |
| 原始网页 HTML | 既有 DOM、样式和行为 | `project-workflow` + HTML 分析 + UI 验收 |
| 截图 | 视觉层级、布局、间距和状态 | `project-workflow` + `product-design:image-to-code` + 前端设计 + Browser |
| Figma | 设计上下文、组件、变量和视觉规范 | `project-workflow` + `figma-design-to-code` + Figma 连接 + React/Vue 约定 |
| 接口文档 | 请求、响应、Schema、状态和错误 | `project-workflow` + `api-design` + `security-review` |
| 原始需求 | 业务目标、用户任务和验收范围 | `project-workflow` + Product Design + 前端设计 + TDD |

输入来源可以自由组合。组合时按事实类型分工：

- 业务事实来自需求、接口文档和项目现有代码。
- 视觉事实来自截图、原型图和 Figma。
- 工程事实来自项目代码、配置、测试和 `PROJECT_PROFILE.md`。
- 外部工具只能补充，不能覆盖项目事实。

### 7.3 典型组合

| 场景 | 最小组合 |
|---|---|
| 截图还原页面 | `project-workflow` + `image-to-code` + `frontend-design` + Browser |
| Figma 转 React | `project-workflow` + `figma-design-to-code` + React Best Practices |
| 页面加 API | 对应 UI 组合 + `api-design` + `security-review` |
| 只有原始需求 | `project-workflow` + Product Design + `frontend-design-direction` + `frontend-design` |
| 增量缺陷修复 | `project-workflow` + `tdd-workflow`，仅在涉及 UI 时增加 Browser/UI 能力 |
| 原始 HTML 改造 | `project-workflow` + HTML 分析 + UI 验收 |
| 新项目页面开发 | 先完成 `bootstrap-project`，再运行 `frontend-task` |


### 7.5 任务开发参考文档必须具体到可执行步骤

`frontend-task/references/` 不能只写抽象路由说明。每个任务类型都应描述输入格式、暂停确认点、产物路径、实现约束、验证命令和验收标准。下面是一比一还原页面任务的规范示例：

```markdown
# 任务：一比一还原页面

## 输入
- 页面 / 模块名：<订单列表 / order>
- 所属路由：< /order/list >
- 设计来源（任选其一）：
  - Figma：<链接>，目标 frame：<名称>
  - 图片：<路径 1>（默认态）、<路径 2>（弹窗态）
  - 截图 CSS 视口宽度：<1440>，DPR：<2>
- 接口资料：见接口块

## 第一步：设计解析（写入 docs/design/<页面>.md，停下等确认）
1. 提取 Token：尺寸、栅格、颜色、字号、字重、行高、间距、圆角和阴影。
2. 输出组件拆分树：组件名、职责、props、emits、复用范围。
3. 输出状态矩阵：默认、悬停、选中、禁用、空、加载、错误、暗色及派生规则。
4. 输出图标清单：设计图标到项目 iconfont 或图标组件的映射，未匹配项进入待补清单。
5. 输出文案清单：文案、i18n key、zh-CN、en-US。
6. 对复杂交互记录借鉴的组件拆分和状态管理思路，不借鉴外部项目视觉。
7. 将不确定估算单独列出，未确认前不得写入最终 Token。

## 第二步：接口与结构（写入接口映射，停下等确认）
- 根据接口块生成 `src/api/<模块>/{types,adapter,index}.ts`。
- 生成 `views/<模块>/config.ts`、常量、i18n key 和必要的 Token 增量。
- 将 VO 字段与设计字段、文案清单逐一对应。
- 单独列出缺失、多余、类型不一致和权限相关字段。

## 第三步：编码
- 仅使用第一步确认的 Token、组件树和状态矩阵。
- 禁止“优化”“美化”或改变设计意图的自主调整。
- 按项目 `AGENTS.md` 门控执行 TDD、Web Guidelines 和 Security Review。
- 不把截图中的视觉推断写成业务规则。
- 完成后在设计稿视口截图，输出 `docs/design/compare/<页面>/` 差异清单；无法运行浏览器时输出逐区域自检清单。

## 验收
- 设计稿尺寸下视觉对比无可见偏移，Token 表每项可定位到代码变量。
- zh-CN / en-US 切换无溢出，英文按 1.3 倍长度预留。
- light / dark 切换无遗漏颜色。
- 断网、空数据、500、权限拒绝和加载状态均可用。
- 桌面、平板、移动视口均完成验证。
- 控制台无新增错误，关键接口请求和错误处理有证据。
```

同类参考文档还应包括：原型转页面、原始 HTML 改造、截图还原、Figma 转代码、接口接入和只有原始需求。每份文档都必须明确“何时停下等确认”，避免 Skill 在设计事实未确认前直接编码。

### 7.4 任务执行闭环

```text
读取 AGENTS.md 和 PROJECT_PROFILE.md
→ 识别输入来源和事实类型
→ 检查已有组件、路由、API 和测试
→ 选择最小 Skill、插件和工具组合
→ 输出计划、风险和待确认项
→ 用户确认高风险决策
→ 实现最小可验证改动
→ 运行针对性测试和项目质量门禁
→ 使用 Browser 做真实 UI 验收
→ 检查控制台、网络、响应式、键盘和错误状态
→ 输出变更、命令、证据和未验证项
```


### 7.6 初始化脚本与前端任务 Skill 的关系

`check-ai-guidance.mjs` 及其 `scripts/lib/` 依赖属于项目初始化和规则接入后的质量门禁，不是前端任务 Skill 的默认运行时依赖。

- `bootstrap-project`：读取通用模板包或项目 `.codex/templates/`，在规则接入后调用，用于检查 `AGENTS.md`、Skill 元数据、文档链接、占位符和配置一致性。
- `frontend-task`：默认读取项目规则并执行项目真实的测试、构建、Lint、类型检查和 Browser 验收命令。
- 只有当本次前端任务修改了 AI 指引、Skill、`AGENTS.md`、`PROJECT_PROFILE.md` 或相关配置时，`frontend-task` 才应按项目规则调用项目 `.codex/scripts/check-ai-guidance.mjs`；如果项目未接入副本，则调用初始化工具包提供的只读校验脚本。
- 如果目标项目尚未接入 `.codex/scripts/`，`bootstrap-project` 应在初始化确认阶段提出是否接入；获得批准后复制并校验脚本，未批准时使用工具包中的脚本进行只读检查，并报告项目未保留本地副本。
- `.codex/scripts` 是否接入目标项目，必须由初始化阶段确认；对新项目默认建议接入，以保证离线可重复校验。业务项目原有的 `scripts/` 目录继续由项目自行管理，不被模板整目录覆盖。

## 8. 多端适配标准

所有涉及 UI 的前端任务，默认至少覆盖：

- 桌面端宽视口。
- 平板端中等视口。
- 移动端窄视口。
- 加载、空数据、错误和权限拒绝状态。
- 键盘操作、焦点可见性和基本可访问性。
- 文本溢出、长内容、窄屏换行和滚动行为。
- 主题、颜色对比度和已有设计 Token。
- 浏览器控制台错误和关键网络请求。

验收不能只依赖静态截图：

- 截图用于视觉比对。
- Browser 用于真实交互、路由、状态、控制台和网络验证。
- 接口验证必须使用安全测试数据，不连接生产写入。
- Mock、HTTP 200、构建成功或截图生成成功都不能单独证明业务流程完成。

## 9. 需要修正的原有设计

1. 不把“完整模式”设为默认模式，默认按任务选择最小组合。
2. 不要求每次安装后都新建会话，只在 Skill 需要重新发现时新建。
3. 不用命令退出码证明 Skill 已被当前会话发现。
4. 分离插件安装、启用、外部连接和任务可用状态。
5. 不让项目初始化自动覆盖已有文件。
6. 不让所有未知字段阻塞初始化，只阻塞高影响字段。
7. 不把视觉来源当作业务事实。
8. 分离截图视觉检查和 Browser 真实验收。
9. 不把示例脚本或 `package-scripts.example.json` 当作项目真实命令。
10. 所有 Markdown 和配置统一使用 UTF-8，修复当前文件中的中文乱码。
11. 项目 Skill 不通过 `npx skills add` 当作全局 Skill 安装，而是随项目文件接入。
12. Figma 和 GitHub 只有在当前任务需要时才要求连接。
13. 对高风险外部写入、发布、删除、通知和生产操作保留明确授权点。

## 10. 实施计划

### 阶段 1：整理和兼容

- 修复现有中文文件的 UTF-8 编码。
- 保留现有文件，补充职责说明和迁移标记。
- 将能力清单、任务合同和验收证据中的重复规则去重。
- 为现有项目建立 `capability-state.yaml` 的初始结构。

### 阶段 2：拆分 Codex 能力安装流程

- 新增 `codex-capability-setup` Skill。
- 将三阶段流程拆到 Skill 和 references。
- 保留 `给Codex的安装提示词.md` 作为兼容入口和迁移说明。
- 使用结构化审计报告和批准输入替代手工复制整张表。

### 阶段 3：拆分项目初始化流程

- 新增 `bootstrap-project` Skill。
- 将画像选择、字段确认、文件合并和校验拆分。
- 保留 `给项目初始化的提示词.md` 作为兼容入口。
- 引入高影响字段优先确认机制。

### 阶段 4：新增前端任务能力

- 新增 `frontend-task` Skill。
- 新增 `docs/AI_FRONTEND_TASK.md` 和任务文件模板。
- 实现输入来源识别和最小能力路由。
- 增加多端 UI 验收矩阵。

### 阶段 5：验证和迭代

至少使用两个真实场景验证：

1. 截图 + 接口文档，实现一个带加载、空态、错误态的响应式页面。
2. 只有原始需求，从需求分析到页面设计、实现和验收。

验证内容：

- Skill 可发现性。
- 项目规则是否被正确加载。
- 任务是否只加载必要能力。
- 现有文件是否被保护。
- 多端视觉和交互是否可验收。
- 测试、构建、静态检查和 AI 指引校验是否通过。

## 11. 交付结果

改造完成后，用户面对的是三个清晰入口：

- **能力安装**：运行 `codex-capability-setup`，完成审计、批准安装和新会话验证。
- **项目初始化**：运行 `bootstrap-project`，完成画像建立、规则接入和初始化校验。
- **前端任务开发**：运行 `frontend-task`，根据输入来源组合完成新项目或增量开发。

这三个入口共享 `project-workflow`、`PROJECT_PROFILE.md`、`AI_PROJECT_STANDARDS.md` 和 `AI_ACCEPTANCE_EVIDENCE.md`，但职责彼此独立，避免安装、初始化和业务开发相互污染。






## 12. 方案完整性与生命周期修正

本节是本方案的实施前置条件，用于确保文件不仅存在，而且确实会被 Codex 在正确阶段读取和执行。

### 12.1 初始化前、初始化中和初始化后的依赖关系

新项目第一次初始化时可能没有 `AGENTS.md`、`docs/PROJECT_PROFILE.md` 或项目级 Skill，因此初始化流程不能反过来依赖这些文件。

正确关系如下：

```text
初始化工具包中的引导协议
  → 读取项目代码、配置和文档
  → 生成或合并 docs/PROJECT_PROFILE.md
  → 生成或合并 AGENTS.md
  → 接入 project-workflow
  → 初始化完成后由 project-workflow 接管日常任务
```

具体规则：

- `bootstrap-project` 必须支持目标项目没有 `AGENTS.md` 的首次初始化模式。
- 初始化前使用通用模板包中的引导协议、模板和校验工具，不假设目标项目已有规则。
- `AGENTS.md` 是初始化产物和后续项目级规则入口，不是初始化的前置依赖。
- `project-workflow` 只有在接入并通过发现性验证后，才作为日常任务的项目工作流。
- `frontend-task` 不负责生成 `AGENTS.md`，发现缺少项目画像时应暂停并建议先运行 `bootstrap-project`。

### 12.2 模板源包与项目副本的生命周期

模板和脚本需要同时满足“新项目可初始化”和“已初始化项目可重复使用”两个目标，因此必须区分两个生命周期。

#### 模板源包

模板源包由本项目或独立模板仓库维护，负责：

- 新项目首次初始化。
- 画像模板选择和生成。
- `AGENTS.md` 规则候选生成。
- 初始化 Skill 和校验脚本的来源。
- 模板版本和迁移说明。

推荐结构：

```text
通用模板包/
├─ templates/
│  ├─ project-profile.react.md
│  ├─ project-profile.vue.md
│  └─ project-profile.generic.md
├─ scripts/
│  ├─ check-ai-guidance.mjs
│  └─ lib/
└─ .agents/skills/
   ├─ bootstrap-project/
   └─ frontend-task/
```

#### 目标项目副本

初始化完成后，目标项目默认建议保留 Codex 专用副本：

```text
目标项目/
├─ .codex/
│  ├─ manifest.yaml
│  ├─ templates/
│  └─ scripts/
├─ .agents/skills/
│  ├─ project-workflow/
│  ├─ bootstrap-project/
│  └─ frontend-task/
├─ AGENTS.md
└─ docs/
```

`.codex/manifest.yaml` 至少记录：

- 模板包名称和版本。
- 来源地址或来源标识。
- 接入时间。
- 已接入的模板、脚本和 Skill。
- 是否允许后续更新。
- 最近一次校验结果。

目标项目已有的业务 `templates/`、`scripts/` 目录继续由项目自行管理；初始化流程不得整目录覆盖。Codex 资源统一使用 `.codex/` 命名空间，避免文件名冲突。

如果用户明确不希望保留本地副本，可以只保留项目 Skill 和来源记录，但每次初始化或校验都需要访问模板源包；这种模式不作为默认方案。

### 12.3 `AGENTS.md` 的生成和更新规则

`AGENTS.md` 的来源关系为：

```text
通用规则候选 + 项目事实 + 用户确认的项目边界
→ 生成或合并目标项目 AGENTS.md
```

初始化时：

1. 读取已有 `AGENTS.md`；不存在时建立空的项目规则基线。
2. 读取项目代码、配置、测试、文档和命令，形成事实清单。
3. 将通用模板内容与项目事实逐段比较。
4. 只写入用户批准或项目事实明确支持的规则。
5. 将项目专属路径、命令、组件、接口和业务边界写入项目规则。
6. 输出新增、保留、冲突和未确认内容。

更新时：

- 不得用新版模板整文件替换已有 `AGENTS.md`。
- 规则冲突必须停下并要求用户确认。
- 项目规则优先级高于通用示例，但不能违反系统级约束和本次用户明确要求。
- 稳定且可重复的规则应继续保留在 `AGENTS.md`，一次性任务内容应留在任务文件或任务 Prompt 中。

### 12.4 所有文档和资源的调用矩阵

| 文件或资源 | 主要使用者 | 读取时机 | 读取后的动作 |
|---|---|---|---|
| `AGENTS.md` | `project-workflow`、`bootstrap-project`、`frontend-task` | 初始化后所有任务；初始化时若存在则读取 | 应用项目级规则和边界 |
| `docs/PROJECT_PROFILE.md` | 三个项目 Skill | 所有任务开始 | 建立项目事实、命令和能力基线 |
| `docs/FRONTEND_CONVENTIONS.md` | `frontend-task`、UI 相关任务 | 涉及前端代码时 | 约束目录、组件、样式、响应式和代码形式 |
| `docs/AI_PROJECT_STANDARDS.md` | `project-workflow`、API、安全和交付流程 | 涉及工程、API、安全或质量时 | 应用项目开发标准 |
| `docs/AI_COMPONENT_CATALOG.md` | `frontend-task` | 页面或组件任务 | 查询复用组件和既有页面结构 |
| `docs/AI_ACCEPTANCE_EVIDENCE.md` | `project-workflow`、`frontend-task` | 实现完成和交付阶段 | 生成验收证据和未验证项 |
| `docs/CODEX_CAPABILITIES.md` | `codex-capability-setup`、`project-workflow` | 能力审计和任务路由 | 查找精确能力名称、来源和安装方式 |
| `docs/AI_WORKFLOW_PRINCIPLES.md` | `bootstrap-project`、`project-workflow` | 初始化、路由和规则维护 | 应用事实分层和执行闭环 |
| `docs/AI_TASK_CONTRACT.md` | 通用任务流程、`frontend-task` | 任务开始 | 解析目标、上下文、约束和完成条件 |
| `docs/AI_FRONTEND_TASK.md` | `frontend-task` | 前端任务开始 | 解析任务输入和来源组合 |
| `docs/capability-state.yaml` | `codex-capability-setup` | 安装和验证阶段 | 记录安装、启用、连接、发现和任务可用状态 |
| `.codex/manifest.yaml` | `bootstrap-project`、能力迁移流程 | 初始化和升级 | 判断模板副本版本和可更新范围 |
| `.codex/templates/` | `bootstrap-project` | 初始化和画像更新 | 提供画像及任务模板副本 |
| `.codex/scripts/check-ai-guidance.mjs` | `bootstrap-project`；规则文件变更时的 `frontend-task` | 初始化后校验或规则变更 | 检查占位符、链接、元数据和配置一致性 |
| `.agents/skills/project-workflow/` | 所有项目任务 | 初始化完成后 | 提供项目事实调查、路由、实现和验证闭环 |
| `.agents/skills/bootstrap-project/` | 初始化和画像更新 | 初始化前、中、更新时 | 生成画像、合并规则和执行初始化校验 |
| `.agents/skills/frontend-task/` | 前端新开发和增量开发 | UI、页面、组件和前端接口任务 | 根据输入来源执行开发闭环 |

任何新增文件都必须加入该矩阵，否则不得作为必需资源加入方案。

### 12.5 初始化脚本与前端任务的调用边界

`check-ai-guidance.mjs` 是初始化和 AI 指引变更的质量门禁，不是普通页面开发的默认命令。

- `bootstrap-project`：规则接入后调用，检查 `AGENTS.md`、Skill 元数据、文档链接、占位符、配置和脚本一致性。
- `frontend-task`：默认执行项目真实的测试、类型检查、Lint、构建和 Browser 验收命令。
- 当任务修改了 `AGENTS.md`、`.agents/skills/`、`docs/PROJECT_PROFILE.md`、AI 指引文档、`ai-guidance.config.mjs` 或校验脚本时，才额外调用 `check-ai-guidance.mjs`。
- 目标项目存在 `.codex/scripts/` 时优先使用本地副本；不存在时使用模板包中的只读校验脚本，并报告项目未保留本地副本。
- 不得因为脚本存在就自动把它加入项目；是否接入由初始化阶段确认，面向新项目默认建议接入。

### 12.6 `frontend-task/references` 的可执行规范

`frontend-task/SKILL.md` 只负责识别任务类型、读取项目事实、选择 references、控制暂停点和组织验收；具体任务文档负责可执行细节。

每份 `references/*.md` 必须包含：

1. 适用场景和不适用场景。
2. 标准输入格式。
3. 输入证据的优先级和冲突处理。
4. 分阶段步骤。
5. 每一步的输出文件或输出表格。
6. 必须暂停并等待用户确认的节点。
7. 允许调用的 Skill、插件和工具。
8. 编码前的完成条件。
9. 测试、Browser 和多端验收要求。
10. 最终交付证据格式。

例如“一比一还原页面”至少应执行：

```text
输入页面、路由、Figma 或截图、视口和 DPR、接口块
→ 设计解析
→ 写入 docs/design/<页面>.md
→ 输出 Token、组件树、状态矩阵、图标和文案清单
→ 对估算值和冲突项暂停确认
→ 接口与结构映射
→ 输出 types、adapter、index、config、常量和 i18n 计划
→ 再次暂停确认
→ 按已确认 Token 和状态矩阵编码
→ 设计稿视口截图和差异记录
→ 桌面、平板、移动、主题、语言、加载、空、错误和权限验收
```

截图只能提供视觉事实，Figma 才能在连接可用时提供设计节点和 Token 事实；两者都不能自动决定业务规则。接口字段必须与设计字段和文案清单逐一映射，多余、缺失和权限字段必须单独报告。

### 12.7 任务来源路由规则

`source-routing.md` 必须定义确定性的加载规则：

```text
有 Figma 链接或 frame
  → 加载 figma-workflow.md
有截图
  → 加载 screenshot-workflow.md
有原型图或导出 HTML
  → 加载 prototype-workflow.md
有原始网页 HTML
  → 加载 html-workflow.md
有接口块或接口文档
  → 加载 api-workflow.md
只有原始需求
  → 加载 requirement-workflow.md
存在多个来源
  → 合并所有匹配 reference，并执行各自确认点
```

来源冲突按以下顺序处理：

1. 系统和项目规则。
2. 用户本次明确要求。
3. 业务和接口事实。
4. 已确认的设计事实。
5. 当前代码和测试事实。
6. 外部工具输出和推断。

无法消解的冲突必须暂停，不得自行选择视觉或业务方案。

### 12.8 旧方案兼容和回归要求

改造不能只完成文件拆分，还必须证明旧行为没有丢失。建议新增：

```text
.codex/
├─ manifest.yaml
├─ compatibility.md
└─ migration-state.yaml
```

其中：

- `compatibility.md` 记录旧 Prompt 每项规则对应的新 Skill、reference 或配置。
- `migration-state.yaml` 记录迁移阶段、已完成项、未迁移项和回归结果。
- 安装流程必须保留“审计→批准→安装→新会话验证”三阶段。
- 初始化流程必须保留“事实读取→字段确认→受保护写入→规则接入→校验”。
- 视觉任务必须保留设计来源、Token、状态、接口、暂停确认和真实浏览器验收。

## 13. 最终可行性判断

完成本节约束后，方案中的主要文件均有明确的使用者、触发时机和输出动作：

- 安装能力由 `codex-capability-setup` 使用能力清单和状态文件完成。
- 新项目初始化由 `bootstrap-project` 使用模板、项目事实和初始化脚本完成。
- 初始化后的日常开发由 `project-workflow` 读取 `AGENTS.md` 和 `PROJECT_PROFILE.md` 完成。
- 前端新开发和增量开发由 `frontend-task` 按输入来源加载具体 reference 完成。
- 项目规则变化由初始化脚本校验，普通页面任务不承担不必要的初始化门禁。
- `.codex/` 保存项目可复用的模板、脚本和迁移元数据，避免与业务目录冲突。
- `docs/` 文件承担事实、约定、任务合同、能力清单和验收证据等不同职责，不再重复承载同一套流程。

因此，改造后的方案可以覆盖旧 Prompt 的全部核心使用效果，并在任务组合、项目复用、离线初始化、多端验收、状态追踪和团队协作方面超过旧方案。最终是否达到这一目标，必须以第 10 节列出的真实场景回归验证和本节的文件调用矩阵检查为准。

## 14. 首次初始化启动协议

新项目可能完全没有 `AGENTS.md`、`.agents/skills/`、`docs/PROJECT_PROFILE.md` 和 `.codex/`。因此首次初始化必须从模板源包或已安装的全局 `bootstrap-project` Skill 启动，不能要求项目先具备初始化后才会生成的文件。

### 14.1 推荐启动方式

在通用模板包目录或其可访问的副本中，向 Codex 明确发起：

```text
使用 bootstrap-project 初始化当前项目。
模板源：<通用模板包路径、Git 仓库或已安装 Skill>
目标项目：<当前工作目录>
```

Codex 按以下顺序执行：

```text
确认目标项目路径
→ 读取模板包中的 bootstrap-project、模板和校验脚本
→ 读取目标项目已有代码、配置、文档和测试
→ 生成 PROJECT_PROFILE 草案
→ 输出字段确认和接入清单
→ 用户确认
→ 写入 AGENTS.md、PROJECT_PROFILE.md、项目 Skill 和 .codex 资源
→ 执行初始化校验
→ 输出迁移和验收报告
```

### 14.2 启动来源优先级

1. 当前项目已存在且可发现的 `.agents/skills/bootstrap-project/`。
2. 当前项目 `.codex/manifest.yaml` 记录的模板源和本地 `.codex/templates/`。
3. 用户明确提供的通用模板包路径或仓库。
4. 已安装的全局 `bootstrap-project` Skill。
5. 如果以上来源均不可用，报告初始化资源缺失，不得猜测模板内容或自行生成完整规则。

### 14.3 初始化完成判定

只有以下条件同时满足，才可报告初始化完成：

- `AGENTS.md` 已生成或完成受保护合并。
- `docs/PROJECT_PROFILE.md` 已写入已确认事实，且没有未确认占位符。
- 用户批准的项目 Skill 已接入并可发现。
- `.codex/manifest.yaml` 已记录来源和版本。
- 已批准的 `.codex/templates/` 和 `.codex/scripts/` 已接入，或明确记录为不接入。
- 初始化校验和项目允许的质量命令已通过。

## 15. 文件调用图

文件必须通过 `AGENTS.md`、Skill 的 `SKILL.md` 或 reference 的显式引用进入调用链。目录中存在文件本身不代表 Codex 会自动读取。

```text
模板源包 / 全局 bootstrap-project
  ├─ templates/project-profile.*.md
  ├─ scripts/check-ai-guidance.mjs
  └─ .agents/skills/bootstrap-project/
                    │
                    ▼
          bootstrap-project/SKILL.md
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
profile-selection  profile-schema  confirmation-flow
        │           │           │
        └───────────┴─────┬─────┘
                          ▼
                docs/PROJECT_PROFILE.md
                          │
                          ▼
                      AGENTS.md
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
 project-workflow   frontend-task   codex-capability-setup
          │               │               │
          ▼               ▼               ▼
 PROJECT_PROFILE   AI_TASK_CONTRACT  CODEX_CAPABILITIES
 FRONTEND_CONVENTIONS AI_FRONTEND_TASK capability-state.yaml
 AI_PROJECT_STANDARDS AI_COMPONENT_CATALOG
 task-routing       source-routing
          │               │
          ▼               ▼
 references/*.md   design / API / test / Browser
          │               │
          └───────┬───────┘
                  ▼
       AI_ACCEPTANCE_EVIDENCE.md
                  │
                  ▼
          交付证据和未验证项
```

调用规则：

- `bootstrap-project/SKILL.md` 必须显式引用画像模板、字段定义、确认流程、合并规则和初始化校验。
- `project-workflow/SKILL.md` 必须先读取 `AGENTS.md` 和 `PROJECT_PROFILE.md`，再按任务路由读取 references。
- `frontend-task/SKILL.md` 必须读取 `AI_TASK_CONTRACT.md`、`AI_FRONTEND_TASK.md`、`FRONTEND_CONVENTIONS.md`，并根据 `source-routing.md` 加载具体工作流。
- 每个前端 reference 必须显式引用 `AI_COMPONENT_CATALOG.md`、项目画像中相关章节和 `AI_ACCEPTANCE_EVIDENCE.md`。
- `codex-capability-setup` 必须读取 `CODEX_CAPABILITIES.md` 和 `capability-state.yaml`，不能只依赖目录扫描。

## 16. 旧规则逐条兼容矩阵

实施改造时必须建立并维护以下兼容矩阵。所有“待验证”项目完成真实回归前，不得宣称完全兼容。

| 旧方案规则 | 新承载位置 | 验证方式 | 状态 |
|---|---|---|---|
| 安装前只读审计 | `codex-capability-setup/references/setup-phases.md` | 审计阶段不产生安装、连接或文件写入 | 待验证 |
| 只安装用户批准项 | `codex-capability-setup/references/install-policy.md` | 使用部分批准输入，确认未批准项未执行 | 待验证 |
| 已安装且来源匹配则跳过 | `install-policy.md` + `capability-state.yaml` | 重复运行安装流程 | 待验证 |
| Browser 属于宿主内置能力 | `CODEX_CAPABILITIES.md` + `verification-checklist.md` | 检查不通过插件目录安装 Browser | 待验证 |
| Figma/GitHub 安装与连接分离 | `status-model.md` + `install-policy.md` | 模拟已安装未连接状态 | 待验证 |
| 新 Skill 安装后新会话验证 | `verification-checklist.md` | 新会话检查精确 Skill 名称和来源 | 待验证 |
| 项目 Skill 不通过全局安装 | `bootstrap-project` + `project-workflow` | 项目内接入后验证可发现 | 待验证 |
| 无 `AGENTS.md` 时可首次初始化 | `bootstrap-project` + 第 14 节 | 空项目初始化演练 | 待验证 |
| 画像字段必须有证据 | `profile-schema.md` + `confirmation-flow.md` | 检查未知字段是否保持待确认 | 待验证 |
| 不覆盖已有画像 | `merge-policy.md` + `protected-files.md` | 使用已有画像和冲突字段演练 | 待验证 |
| 不覆盖已有 AGENTS 规则 | `merge-policy.md` | 使用已有规则和模板冲突演练 | 待验证 |
| 模板不整目录复制 | `protected-files.md` | 目标存在同名目录时检查差异 | 待验证 |
| 视觉事实不覆盖业务事实 | `source-routing.md` + 各视觉 reference | 设计稿与接口冲突演练 | 待验证 |
| 设计解析后暂停确认 | `prototype-workflow.md`、`screenshot-workflow.md`、`figma-workflow.md` | 确认前不得编码 | 待验证 |
| 接口字段与设计字段映射 | `api-workflow.md` | 检查缺失、多余和权限字段报告 | 待验证 |
| UI 真实浏览器验收 | `acceptance-matrix.md` + Browser 能力 | 检查路由、交互、控制台和网络 | 待验证 |
| 不执行生产写入或外部提交 | `AGENTS.md` + `AI_PROJECT_STANDARDS.md` | 高风险动作演练 | 待验证 |

## 17. Skill 触发和组合矩阵

Skill 的触发由任务类型、输入来源和变更范围共同决定，不因“前端任务”四个字而全部加载。

| 场景 | 必需 | 按需增加 | 默认不加载 |
|---|---|---|---|
| 读取项目、诊断或普通代码修改 | `project-workflow` | `tdd-workflow` | Product Design、Figma |
| 新页面或视觉重做 | `project-workflow`、`frontend-task`、`frontend-design` | Product Design、Browser、Web Guidelines | Figma（无 Figma 来源时） |
| 截图一比一还原 | `project-workflow`、`frontend-task`、`image-to-code` | `frontend-design`、Browser、Web Guidelines | Figma（无链接时） |
| 原型图或导出 HTML | `project-workflow`、`frontend-task` | 原型/HTML reference、Browser | image-to-code（无截图时） |
| Figma 转代码 | `project-workflow`、`frontend-task`、`figma-design-to-code` | React/Vue 约定、Browser | image-to-code |
| 页面接入接口 | 上述 UI 组合、`api-design` | `security-review`（认证、权限、敏感数据时） | GitHub |
| 只有原始需求 | `project-workflow`、`frontend-task` | Product Design、`frontend-design-direction`、`frontend-design`、`tdd-workflow` | Figma |
| 增量缺陷修复 | `project-workflow`、`tdd-workflow` | Browser、Web Guidelines | Product Design |
| 规则、Skill 或 AI 文档变更 | `project-workflow` | `check-ai-guidance.mjs`、Skill validator | Browser（无 UI 变更时） |
| GitHub 远程读取或写入 | `project-workflow` | GitHub 插件 | 默认不连接 GitHub |

触发补充规则：

- `security-review` 在认证、授权、用户输入、文件上传、敏感数据、支付或新 API 场景触发。
- `tdd-workflow` 在新功能、缺陷修复、重构和 API 变更触发；纯视觉微调可由项目规则定义针对性测试例外。
- `web-design-guidelines` 在 UI 审查、可访问性检查或交付前 UI 质量检查触发。
- Browser 在真实 UI 路由、交互、控制台、网络或响应式验收需要时触发。
- Figma 插件只有存在 Figma 来源或用户明确要求读写 Figma 时触发。
- GitHub 插件只有远程仓库、Issue、PR 或外部资料检索需要时触发。

## 18. 新项目初始化验收场景

至少执行以下场景，验证首次初始化不依赖预先存在的项目规则：

### 场景 A：空目录新项目

输入：空目录、React 或 Vue 模板选择、用户确认的运行时和命令。

验收：

- 能从模板源包启动。
- 生成 `AGENTS.md`、`PROJECT_PROFILE.md` 和项目 Skill。
- 生成 `.codex/manifest.yaml`。
- `.codex/templates/` 和 `.codex/scripts/` 按确认接入。
- 初始化校验通过。
- 后续新会话能发现 `project-workflow`。

### 场景 B：已有代码但无项目规则

输入：已有 `package.json`、源码、测试和配置，但无 `AGENTS.md` 与项目画像。

验收：

- 读取真实代码和命令，不把模板示例当成事实。
- 对无法确认的字段暂停。
- 生成画像和规则后才允许运行 `frontend-task`。

### 场景 C：已有画像和规则的项目

输入：已有 `AGENTS.md`、`PROJECT_PROFILE.md`、业务文档和自定义脚本。

验收：

- 保留既有规则和业务事实。
- 只输出差异和冲突。
- 不整目录覆盖 `templates/` 或 `scripts/`。
- 用户未批准时不接入新的约定或脚本。

## 19. 前端任务回归场景

至少执行以下真实任务，验证旧能力未丢失且新组合有效：

### 场景 A：截图 + 接口文档

目标：实现响应式订单列表。

输入：默认态和弹窗态截图、CSS 视口与 DPR、接口文档。

验收：

- 设计解析先写入 `docs/design/<页面>.md` 并暂停。
- Token、组件树、状态矩阵、图标和文案清单完整。
- 接口字段与 UI 字段逐一映射。
- 断网、空数据、500、权限拒绝和加载态可验证。
- 桌面、平板、移动视口完成 Browser 验收。
- 视觉差异写入 `docs/design/compare/<页面>/`。

### 场景 B：只有原始需求

目标：从需求设计并实现一个新页面。

验收：

- 先识别用户目标、业务边界和验收条件。
- 缺少视觉来源时生成设计方向并等待确认。
- 复用项目组件目录和已有 Token。
- 业务状态、接口契约和权限边界有明确来源。
- 完成针对性测试、构建、Lint、类型检查和 Browser 验收。

### 场景 C：Figma + 接口文档

目标：将已确认的 Figma frame 实现为项目页面。

验收：

- Figma 读取和代码实现分阶段执行。
- Figma Token 与项目 Token 对齐，冲突单独报告。
- 不把 Figma 视觉节点当作业务规则。
- API、权限、错误状态和多端适配均有证据。

### 场景 D：原始网页 HTML 增量改造

目标：在既有页面上改造局部模块。

验收：

- 保留既有路由、业务逻辑和可复用组件。
- 先输出 HTML 结构和影响范围。
- 只修改用户批准的模块。
- 回归原有交互、测试和响应式行为。

## 20. 最终可行性与发布门槛

在完成实际改造前，只能将方案状态标记为“设计完成、实现待验证”。

只有同时满足以下条件，才能将状态升级为“兼容旧方案并超越”：

- 文件调用图中的必需引用已经写入对应 Skill。
- 旧规则兼容矩阵全部完成验证，且没有未解释的缺口。
- 空目录新项目初始化通过。
- 已有项目画像和规则的受保护合并通过。
- 截图 + 接口文档任务回归通过。
- 只有原始需求任务回归通过。
- Figma 或 HTML 组合任务至少完成一种回归。
- 任务只加载必要能力，未出现无条件全量加载。
- 初始化脚本、测试、构建、Lint、类型检查和 Browser 验收结果都有真实证据。
- 未经批准的安装、连接、文件覆盖、外部提交和生产写入均未发生。

达到上述门槛后，改造方案才能证明：它完全保留旧 Prompt 的安装、初始化、事实保护和验收效果，并在可组合任务、项目复用、离线资源、多端验收、状态追踪和团队协作方面实现超越。
