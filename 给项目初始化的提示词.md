# 给项目初始化的提示词

请执行一次性的项目级初始化：选择或生成 `PROJECT_PROFILE`，并在用户确认后接入 Codex 项目规则。此 Prompt 不执行业务需求，不负责 Codex 新环境的插件、Skill 安装或连接。

重要：如果把 `通用模板/` 整目录直接复制到目标项目，模板中的 `docs/PROJECT_PROFILE.md` 可能覆盖刚生成或已有的项目画像。本 Prompt 禁止整目录覆盖；`docs/PROJECT_PROFILE.md` 是受保护文件，必须按逐字段合并规则处理。

## 适用时机

- 项目画像是每个项目初始化一次的长期资产。
- 如果本次任务只是新 Codex 环境能力审计、安装或新对话发现，请使用 `给Codex的安装提示词.md` 的前三个阶段；不要在那些阶段自动生成或覆盖项目画像。
- 本 Prompt 可以在前三阶段完成后使用，也可以在已有 Codex 环境中单独使用。

## 读取范围

先读取并记录事实来源，不修改任何文件：

- `README.md`
- `给Codex的安装提示词.md`
- `通用模板/`
- `来源映射.md`
- `设计说明.md`
- `通用模板/docs/PROJECT_PROFILE_VUE.md`
- `通用模板/docs/PROJECT_PROFILE_REACT.md`
- `通用模板/docs/PROJECT_PROFILE.md`
- `通用模板/docs/FRONTEND_CONVENTIONS.md`
- 当前项目的 `AGENTS.md`
- 当前项目的 `.agents/skills/project-workflow/`
- 当前项目的 `docs/PROJECT_PROFILE.md`

同时检查项目代码、`package.json`、锁文件、`.nvmrc`、CI 配置、接口文档、设计文档、测试配置和部署配置。文件缺失或不可读时标记“事实源缺失”，不得猜测。

## 第一步：选择画像来源

### 目标画像不存在时

检查 `docs/PROJECT_PROFILE.md`：

- 如果不存在，向用户提供以下四个选项，不得根据目录名或依赖默认选择：
  1. `Vue 模板`：使用 `通用模板/docs/PROJECT_PROFILE_VUE.md`。
  2. `React 模板`：使用 `通用模板/docs/PROJECT_PROFILE_REACT.md`。
  3. `通用模板`：使用 `通用模板/docs/PROJECT_PROFILE.md`。
  4. `自动生成`：根据项目代码、配置、文档和测试生成同等结构的画像草稿。
- 选项对应文件不存在时，标记“事实源缺失”，不得用相似文件替代。
- 用户未明确选择前，只输出选项状态并停止。

### 目标画像已存在时

如果 `docs/PROJECT_PROFILE.md` 已存在，将其视为当前项目的既有事实源，兼容它可能由用户、脚本或其他 AI 自动生成：

- 不覆盖、不重建、不改写原文件。
- 将现有画像与用户选择的 Vue/React/通用模板或自动生成结果逐字段比较。
- 默认推荐“以现有画像为主，补充缺失字段”；只有用户明确选择模板重构时，才提出整体替换草案。
- 既有非占位内容、业务规则、权限规则和已确认命令优先保留；疑似错误只列为冲突，不擅自修正。

## 第二步：建立画像草稿

无论选择模板还是自动生成，都必须保留目标画像的完整结构，并为每个字段建立状态：

- `已由仓库事实确认`：有明确文件、配置、代码或命令证据，可直接填写。
- `仓库可推断，需用户确认`：存在合理候选值，但会影响架构、体验、权限或发布，不能直接定案。
- `事实缺失，必须用户提供`：仓库没有足够依据，推荐值只能作为讨论起点。
- `不适用`：只有用户确认或项目证据明确不适用时才能填写。

禁止以下行为：

- 把模板示例当成当前项目事实。
- 把“待填写”改成看似具体但没有证据的版本、地址、账号、权限或命令。
- 因为发现 Vue/React 依赖就擅自决定完整技术架构。
- 因为纯中后台就自动判定移动端不适用。
- 因为命令退出码为 0 就宣称能力已安装、已连接或项目已验收。

## 第三步：逐字段确认并给出推荐值

对模板中的每一个未填充、冲突或高风险字段，输出以下表格。不得只列问题而不提供推荐值；无法安全推荐时，推荐值写“无法根据现有事实推荐，必须由用户提供”，并说明缺失原因。

| 章节 | 字段 | 当前值/状态 | 推荐值 | 推荐依据 | 风险或影响 | 用户确认 |
| --- | --- | --- | --- | --- | --- | --- |
| 例如：3. 技术与目录 | Node 版本 | 未发现 `.nvmrc` | 无法安全推荐，建议由项目负责人确认 | package.json 未声明 engines，CI 也未声明 | 影响本地、CI 和构建一致性 | 待确认 |

推荐值必须遵循以下规则：

- 版本、命令、目录和依赖：优先引用实际配置；没有证据时只能给候选范围或明确要求用户提供。
- 身份与权限：优先推荐服务端会话、令牌声明或权限服务作为来源，客户端只做体验限制；不能推荐客户端自报角色。
- 数据与秘密：优先推荐环境变量或批准的秘密管理系统，禁止把真实令牌写入画像。
- API：有后端文档时推荐“按版本审查，不修改契约”；无文档时推荐“生成草案并标记待后端确认”。
- 视口与多端：分别推荐支持范围、响应式断点和验收视口；不能用单个手机尺寸代表全部移动端，也不能只用 1920px 代表桌面端。
- 测试和质量命令：先核对 `package.json`、CI 或项目文档；不存在的脚本推荐为“无”或“待项目补充”，不能虚构可执行命令。
- UI 视觉：设计源已定义的视觉不擅自优化；设计源未覆盖的状态可提出候选，但必须标记为待确认。
- 高风险动作：只推荐验证到授权边界，删除、发布、通知和生产写入必须保留明确授权点。

逐字段确认至少覆盖：

- 基本信息：项目名称、业务目标、目标用户、AI 责任边界。
- 事实源：业务、视觉、工程、身份与权限来源及冲突处理。
- 技术目录：运行时、框架、UI、状态、请求、Schema、测试、源码、组件、业务域、国际化、主题和生成目录。
- 前端领域约定（适用时）：图标、国际化、主题 token、VO/adapter、配置化字段、四态、代码卫生和文档产物入口。
- 硬边界：基础设施、禁止能力、生成目录、秘密、身份权限、不可逆操作、兼容性和发布。
- 质量命令：格式、静态检查、单元、集成、覆盖率、构建、安全、AI 校验、总门禁、E2E。
- UI 验收：支持端、断点、桌面/平板/移动视口、主题、状态、交互、可访问性、布局、浏览器和控制台。
- 接口验收：非生产环境、测试账号、主流程、失败超时、权限拒绝、刷新持久化、高风险动作和接口特有场景。
- 能力映射：项目 Skill、TDD、安全、UI、设计源、浏览器、API、代码托管和发布能力。
- 维护信息：负责人、最近核对日期、重大变更记录。

## 第四步：等待用户确认

完成逐字段表格后停止，不写入文件。输出以下确认输入：

```text
PROJECT_PROFILE 来源：<Vue 模板/React 模板/通用模板/自动生成/现有画像补充>
现有画像处理：<保留并补充/按批准字段合并/暂不修改>
推荐值确认：<全部按推荐值确认/仅确认以下字段>
字段修改：<字段 = 用户指定值；没有则写“无”>
不适用确认：<字段列表；没有则写“无”>
规则模板接入：<批准接入/暂不接入>
前端约定接入：<适用并批准接入/适用但暂不接入/不适用>
画像写入：<批准写入 docs/PROJECT_PROFILE.md / 暂不写入>
```

用户可以回复“全部按推荐值确认”，但 Codex 仍必须保留逐字段表格，确保确认范围可审计。未确认字段不能写入为最终事实。

## 第五步：确认后写入与接入

用户返回确认输入后，重新读取相关文件并执行以下规则：

1. 只写入已由仓库事实确认或用户明确确认的值。
2. 用户确认“不适用”的字段写成“不适用”，不得留空。
3. 仍有未确认字段时停止写入，报告字段和阻塞原因。
4. `docs/PROJECT_PROFILE.md` 不存在时：
   - Vue/React/通用模板选择，复制对应模板并写入确认值。
   - 自动生成选择，写入同等结构的自动生成画像。
5. `docs/PROJECT_PROFILE.md` 已存在时：
   - 只合并用户明确批准的字段。
   - 保留已有非占位内容和业务规则。
   - 不执行未经批准的整体替换。
6. 只有用户明确批准规则接入时，才处理 `AGENTS.md`、`.agents/skills/project-workflow/`、`docs/` 指导文档、`ai-guidance.config.mjs` 和校验脚本；已有文件先输出差异，不覆盖。`docs/PROJECT_PROFILE.md` 始终按第六步的受保护规则处理，不得被模板文件覆盖。
7. 不安装插件或 Skill，不连接 Figma/GitHub，不修改业务代码，不执行生产写入。
8. 写入后重新检查画像：不存在未确认的 `<待填写：...>`、空字段或无证据的伪具体值，才可报告画像初始化完成。

## 第六步：规则模板接入操作

画像确认并获得“规则模板接入：批准接入”后，执行以下初始化操作。该步骤仍不执行业务需求：

### 6.1 读取和检查

读取：

- `README.md`
- `给Codex的安装提示词.md`
- `通用模板/`
- `通用模板/docs/FRONTEND_CONVENTIONS.md`
- `来源映射.md`
- `设计说明.md`

检查目标项目是否已有：

- `AGENTS.md`
- `.agents/skills/project-workflow/`
- `docs/PROJECT_PROFILE.md`

同时检查模板接入涉及的 `docs/`、`ai-guidance.config.mjs` 和 `scripts/` 文件是否已存在。缺失、路径不一致或无法读取时，标记“事实源缺失”，不得猜测或用相似文件替代。

### 6.2 建立待接入清单和覆盖判定

先输出每个来源文件到目标路径的清单，标记：`新增`、`已有且一致`、`已有需合并`、`冲突待确认`。

以下路径属于受保护文件：

- `docs/PROJECT_PROFILE.md`：永不被模板覆盖；只合并用户确认的字段。
- 已有 `AGENTS.md`：保留原有规则，新增内容必须逐段比较。
- 已有 `.agents/skills/project-workflow/`：保留现有 Skill、元数据和引用，冲突时停止确认。
- 已有 `docs/` 指导文档、配置、脚本和业务文件：不覆盖，不因模板版本较新而自动替换。

只有目标路径不存在时，才可以从 `通用模板/` 创建对应文件。目标路径已存在时，必须先输出差异并等待用户确认；不得使用整目录复制、强制覆盖或删除后重建。

### 6.3 实际接入范围

按用户批准和项目情况接入：

- `AGENTS.md`
- `.agents/skills/project-workflow/`
- `docs/` 下必要的项目指导文档
- `docs/FRONTEND_CONVENTIONS.md`（仅当前端约定接入获批准且目标项目尚不存在时）
- `ai-guidance.config.mjs`
- `scripts/check-ai-guidance.mjs`
- `scripts/lib/ai-guidance-validation.mjs`
- `scripts/lib/ai-guidance-validation.test.mjs`

`通用模板/package-scripts.example.json` 只是示例文件，只有项目确实需要且用户明确批准时才接入；不得因为模板存在就创建 `package.json` 或修改现有包脚本。

### 6.4 画像与模板的合并顺序

1. 先完成 `docs/PROJECT_PROFILE.md` 的用户确认和写入，再处理其他模板文件。
2. 其他模板文件接入过程中不得再次复制或替换 `docs/PROJECT_PROFILE.md`。
3. 若模板文档引用画像，引用目标必须保持为当前项目的 `docs/PROJECT_PROFILE.md`。
4. 若模板配置要求严格模式，先确认画像中没有未确认占位符，再修改 `allowPlaceholders` 或包脚本门禁。
5. 前端约定只有在“前端约定接入：适用并批准接入”时写入；同时把画像中的前端约定入口和启用条件改为已确认值。
6. 合并后重新检查本地 Markdown 链接、Skill 元数据、命令引用和项目画像字段。

### 6.5 接入边界

- 保留已有项目规则和业务文件。
- 不修改业务代码、业务配置、业务测试或生成目录。
- 不安装未批准的插件或 Skill，不连接 Figma/GitHub。
- 不创建外部对象、Issue、PR、提交、发布或生产写入。
- 只运行与规则接入直接相关的本地校验命令。

## 交付报告

完成后报告：

- 选择的画像来源和选择理由。
- 每个字段的确认结果和未完成项。
- 推荐值被用户修改的字段及影响。
- 实际变更文件、保留文件和未覆盖文件。
- 规则接入状态。
- 实际运行的校验命令及结果。
- 未安装、未连接、未验证和待后续确认的能力。

不得把模板存在、文件写入成功、构建成功或命令退出码为 0 单独当作项目画像或项目初始化完成的证据。
