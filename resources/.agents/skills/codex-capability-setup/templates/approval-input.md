# 能力安装批准输入

在审计完成后填入具体候选，不让用户重复提供已知事实。既有会话授权覆盖的项目登记为“已授权”，只询问新增范围。

```text
批准模式：<core/ui/react-ui/figma/github/full/按需组合>
批准插件：<精确引用；没有则无>
批准独立 Skill：<精确名称 + 固定仓库/路径；没有则无>
批准连接：<Figma/GitHub/无>
状态记录：<目标项目路径/docs/capability-state.json，或仅对话报告>
排除项：<不安装、不连接的项目；没有则无>
```

该输入只授权列出的安装、连接和状态记录，不授权初始化项目或外部业务写入。
