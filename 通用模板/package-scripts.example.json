{
  "_说明": "把 scripts 字段合并进项目已有 package.json，不要直接覆盖原文件。",
  "scripts": {
    "ai:check": "node scripts/check-ai-guidance.mjs",
    "ai:test": "node --test scripts/lib/ai-guidance-validation.test.mjs",
    "check": "npm run ai:check && <待填写：项目已有质量命令>"
  },
  "_启用严格模式": "填写项目画像后，将 ai-guidance.config.mjs 的 allowPlaceholders 改为 false，并将 packageIntegration.required 改为 true。"
}

